# insert-query-generator

Hi,
You can generate a lot of random inserts and save it in a sql file and add it to your database for testing purposes.


Hola,
Puedes generar muchas insersiones aleatorias y guardarlas en un archivo sql para agregarlo a tu BD de prueba.

Make sure to install tqdm module.
-> pip install tqdm
